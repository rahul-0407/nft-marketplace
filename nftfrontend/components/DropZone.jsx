'use client'

import React,{useState, useMemo, useCallback, useContext} from 'react'
import { useDropzone } from 'react-dropzone/.'

const DropZone = ({title, heading, subHeading, itemName, website, description, fileSize, category, properties, image}) => {
  return (
    <div>DropZone</div>
  )
}

export default DropZone